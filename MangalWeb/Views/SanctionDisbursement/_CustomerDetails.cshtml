@model IList<MangalWeb.Model.Transaction.SanctionDisbursementVM>
@using MangalWeb.Model.Transaction
<div class="modal-content">
    <div class="modal-body" style="height:inherit; overflow:auto;">
        <div class="panel-body panel-no-padding" style="height:inherit; overflow:auto;">
            <div id="demo">
                <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered table-hover table-fixed-header m0" id="example">
                    <label style="margin-left: 320px"><b>Pending S & D : @Model.Count()</b></label>
                    <thead>
                        <tr>
                            <th>Customer ID</th>
                            <th>Applied Date</th>
                            <th>Gold Loan No.</th>
                            <th>Customer Name</th>
                            <th>PAN No</th>
                            <th>Mobile No</th>
                            <th>Loan Type</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (SanctionDisbursementVM item in Model)
                        {
                            <tr class="gradeX">
                                <td id="@item.KYCID">@item.CustomerID</td>
                                <td id="@item.PreSanctionId">@item.AppliedDate</td>
                                <td>@item.LoanAccountNo</td>
                                <td>@item.CustomerName</td>
                                <td>@item.PANNo</td>
                                <td>@item.MobileNo</td>
                                <td>@item.LoanType</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div><!-- /.modal-content -->
